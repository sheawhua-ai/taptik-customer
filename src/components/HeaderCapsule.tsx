import { MoreHorizontal, CircleDot } from 'lucide-react';

export default function HeaderCapsule({ dark = false }: { dark?: boolean }) {
  // Mini program capsule removed as per H5 migration
  return null;
}
